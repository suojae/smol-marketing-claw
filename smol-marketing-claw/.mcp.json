{
  "mcpServers": {
    "smol-claw": {
      "type": "stdio",
      "command": "python3",
      "args": ["-m", "server.mcp_server"],
      "cwd": "${CLAUDE_PLUGIN_ROOT}",
      "env": {
        "PYTHONPATH": "${CLAUDE_PLUGIN_ROOT}/.."
      }
    }
  }
}
